{
  "name": "neewuier",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test src/lib/hub/*.test.ts",
    "test:protocol": "npm test",
    "typecheck": "tsc --noEmit",
    "check": "npm test && npm run typecheck"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "typescript": "^5.8.2"
  }
}
